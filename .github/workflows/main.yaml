# This is a basic workflow to help you get started with Actions
name: Connect to an AWS role from a GitHub repository

# Controls when the action will run. Invokes the workflow on push events but only for the main branch
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  AWS_REGION: eu-central-1

# Permission can be added at job level or workflow level
permissions:
  id-token: write # This is required for requesting the JWT
  contents: read # This is required for actions/checkout
jobs:
  PutSiteOnEC2:
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v7
      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::808122334604:role/github-portfolio
          role-session-name: GitHub_to_AWS_via_FederatedOIDC
          aws-region: ${{ env.AWS_REGION }}

      - name: Put site on S3 and generate presigned URL
        run: |
          aws s3 cp $GITHUB_WORKSPACE/index.html s3://static-site-staging-s3/index.html

          PRESIGNED_URL=$(aws s3 presign s3://static-site-staging-s3/index.html --expires-in 200)

          echo "::add-mask::$PRESIGNED_URL"
          echo "url=$PRESIGNED_URL" >> $GITHUB_OUTPUT

      - name: get instance ID
        run: >
          echo "EC2_INSTANCE=$(aws ec2 describe-instances
          --filter "Name=tag:Name,Values=static-site-ec2"
          --query "Reservations[0].Instances[0].InstanceId"
          --output text)" >> $GITHUB_ENV
